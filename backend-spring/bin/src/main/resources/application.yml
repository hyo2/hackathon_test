server:
  port: 8080
ai:
  base-url: ${AI_BASE_URL:http://localhost:8000}
management:
  endpoints:
    web:
      exposure:
        include: health,info
springdoc:
  api-docs:
    path: /api/docs
  swagger-ui:
    path: /swagger
